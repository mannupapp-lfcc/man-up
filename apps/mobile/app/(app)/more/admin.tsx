import { ComingSoon } from "@/components/ComingSoon";

export default function MinistryAdmin() {
  return (
    <ComingSoon title="Ministry admin" slice={8}>
      Approve roster change requests and glance at every group's health. Everything else lives on the admin website.
    </ComingSoon>
  );
}
